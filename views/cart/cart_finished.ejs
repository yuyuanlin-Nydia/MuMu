<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>購票車_已結帳</title>
    <link rel="stylesheet" href="../../css/cart.css">
    <link rel="stylesheet" href="../../css/mutual.css">
    <link rel="stylesheet" href="../../css/Bootstrap/bootstrap.css">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.3/css/all.css"
        integrity="sha384-SZXxX4whJ79/gErwcOYf+zWLeJdY/qpuqC4cAa9rOGUstPomtqpuNWT9wdPEn2fk" crossorigin="anonymous">
    <script src="../../js/jquery.js"></script>
    <style>
        .box>div>.sub_title  {
            border-bottom: 2px solid var(--color2);
            margin: 20px 0 10px 0;
        }
        .box>div:nth-of-type(1){
            border-bottom: 2px solid var(--color2);

            margin-bottom: 20px;
        }
        .box>div>p{
            font-size: 20px;
        }

        .order_details>ul{
            background-color: var(--color4);
            padding: 50px;    
        }
    </style>
</head>

<body>
    <%-include("../nav.ejs")%>
        
        <main>
            <div class="box">
                <ol class="step">
                    <li>
                        購票車
                    </li>
                    <i class="fas fa-chevron-right"></i>
                    <li>
                        付款
                    </li>
                    <i class="fas fa-chevron-right"></i>
                    <li id="now">
                        完成訂票
                    </li>
                </ol>   
                <div>
                    <p class="sub_title text-center p-3">購票成功</p>
                    <p class="text-center p-3">感謝您的訂票!<br>
                        以下是您的訂單編號及付款明細，<br>
                        可到<b>我的票券</b>去確認您的詳細訂單!</p>
                </div>
                <div class="order_details">

                    <ul> 
                        <li>訂單編號:<%=result[0].orderId%></li>
                        <% var d=result[0].orderDate;
                        var weekDay = ["日", "一", "二", "三", "四", "五", "六"]; %>
                        <li>訂購時間:<%=result[0].orderDate.toLocaleDateString() %> (<%= weekDay[d.getDay()] %>)
                            <%=d.getHours().toString().padStart(2,"0") %>:<%=d.getMinutes().toString().padStart(2,"0") %>

                        <li>信用卡號:<%=result[0].card1 %> - <%=result[0].card2.replace( /\d{4}/ ,"****")
                            %> - <%=result[0].card3.replace( /\d{4}/ ,"****") %> -
                                <%=result[0].card4 %></li>
                        <li>取票方式:<%=result[0].method %></li>
                        <li>總金額:<span class="text-danger">NT$<%= result2[0].sum %></span></li>
                    </ul>
                </div>
                <div class="text-center mt-4">
                    <a href="/activity">
                        <button class="btn_tea1">回活動列表</button>
                    </a>
                    <a href="/user">
                        <button class="btn_tea1">我的票券</button>
                    </a>
                </div>
            </div>
        </main>
        <%-include("../footer.ejs")%>

</body>

</html>